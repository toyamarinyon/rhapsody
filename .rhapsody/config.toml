[runner.codex]
model = "gpt-5.2"
reasoning_effort = "medium"

[post_run]
auto_merge_success_status = "Done"
human_review_status = "Human Review"

[[post_run.auto_merge_eligible]]
paths = ["docs/**", "!docs/adr/**"]
description = "Documentation-only changes with ADR documentation excluded from auto-merge."
