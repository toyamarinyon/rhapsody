とりあえずReadmeをcommitして、pr作ってmergeしてください。

---

configは修正しました。mainにmerge済みです。

---

> .rhapsody/INSTRUCTIONS.md の読み込み元を、修正後の実装に合わせて明記

やったので見てみてください。

---

そうですね。fakeはテスト用なので、これでいいと思っています。

---

では、次は何をしましょう

---

migrateというかdbレイヤ、そろそろdrizzle必要ですかね？

---

そうですね。ただ、migrationがapi実行なのは気持ち悪さがあるので、`pnpm db:migrate`みたいな感じでやりたいですね。
tsxを使って、typescriptでmigrationを実行する感じです。どう思います？

---

[@builder](subagent://builder) に実装してもらいましょう。あなたはゴールを設定して、アウトプットを評価し、十分な水準に達するまでフィードバックを繰り返してください。

---

Implement a TypeScript/tsx-powered database migration CLI for this repo. Ownership: package.json and a new scripts/db-migrate.ts file only unless you discover a compelling reason to touch something else. You are not alone in the codebase; do not revert or overwrite unrelated changes, and accommodate existing work.

Requirements:
- Add a package script named `db:migrate` that runs a TypeScript script through `tsx`.
- Add `tsx` as a devDependency if it is not already available.
- The script should load Next-style env files using `@next/env` from process.cwd(), then use existing state-store helpers (`createStateStoreClient`, `migrateStateStore` or equivalent) to run the existing migrations.
- It must not log secret values.
- It should print a concise success summary including applied migration ids/count, and exit non-zero with a clear message on failure.
- Prefer existing code paths and types. Keep the implementation small and boring.
- Run focused verification if possible, e.g. package script invocation against a temporary local libSQL file or at least typecheck if practical.

Final response: summarize changed files, verification run, and any caveats.

---

Review feedback: typecheck fails with `scripts/db-migrate.ts(1,31): error TS2307: Cannot find module '@next/env' or its corresponding type declarations.` Please fix this cleanly. Keep ownership to package.json, pnpm-lock.yaml, scripts/db-migrate.ts unless absolutely necessary. Options: add @next/env as an explicit dependency/devDependency if appropriate, or use an existing stable Next import that typechecks. After fixing, run `pnpm typecheck` and `pnpm lint` if possible. Also note that I verified `pnpm db:migrate` works with a temp file DB when run outside sandbox restrictions, so preserve the CLI behavior.