[$rhapsody-diagnostics](/Users/toyamarinyon/Documents/rhapsody/.agents/skills/rhapsody-diagnostics/SKILL.md) を使って [https://github.com/toyamarinyon/rhapsody/issues/79](https://github.com/toyamarinyon/rhapsody/issues/79) の状態を教えて欲しい

---

ROOT_PASSWORD `REDACTED`

---

Codexのログも保存されていると思うのですが、何かわかることはありますか？

---

おおいいですね。そういう原因が知りたかったです。実装自体はもうちょっと早く終わっていて、検証に時間がかかっている？

---

今のbuilder sandboxだとpnpm installできないですかね？ネットワーク制限しているのであり得るかもしれません。

---

Common allowed destinationsってどんなのがありますかね？

---

https://developers.openai.com/codex/cloud/internet-access#common-dependencies これが参考になりそう。

---

いいですね。 https://developers.openai.com/codex/agent-approvals-security#network-access のnetwork_policyのような設定が .rhapsody/config.toml　にできると良いと思います。

---

まず設定イメージを擦り合わせたいです。

---

codex cloudみたいなcommon presetを1行で有効にしたい

---

[sandbox.network_policy]
preset = "common_dependencies"

[sandbox.network_policy.domains]
"registry.npmjs.org" = "allow"

これいいと思うんですが、 "registry.npmjs.org" = "allow" は common_dependenciesに含まれているので、この設定自体は無効という理解で良いですか？

---

okです。 [@builder](subagent://builder) に実装してもらってください。

---

Implement Rhapsody sandbox network policy config in /Users/toyamarinyon/Documents/rhapsody.

Context/agreed config shape:

```toml
[sandbox.network_policy]
preset = "common_dependencies"

[sandbox.network_policy.domains]
"registry.npmjs.org" = "allow"
```

Desired behavior:
- Section omitted means existing behavior unchanged: only Rhapsody-required callback/Codex/GitHub policy.
- `preset = "common_dependencies"` adds a Codex Cloud common dependencies style allowlist sufficient for package managers. Definitely ensure `registry.npmjs.org` works. Prefer `**.domain` style internally if useful so apex and subdomains are covered when translating to Vercel Sandbox policy.
- `[sandbox.network_policy.domains]` entries merge with preset entries. Duplicate allow is fine/no-op.
- Initial implementation may support only `allow`; reject unsupported actions clearly.
- Wire parsed policy into `runSandboxCodexRunner` so dependency hosts are merged into the Vercel Sandbox network policy along with existing Codex/GitHub policies.
- Add focused tests for parsing, preset expansion, duplicate/no-op, unsupported action, and Vercel Sandbox policy translation if practical.
- Update `.rhapsody/config.toml` to enable `preset = "common_dependencies"`.

Relevant files to inspect: `lib/runner-codex-config.ts`, `lib/post-run-decision.ts`, `lib/sandbox/vercel.ts`, `lib/runners/sandbox-codex.ts`, `.rhapsody/config.toml`, adjacent tests.

Repo rules: you are not alone in the codebase; do not revert unrelated edits. Edit files directly and report changed paths plus verification commands/results.

---

continue

---

continue?

---

continue